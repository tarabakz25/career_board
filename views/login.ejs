<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>ログイン | Career Board</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700&family=Space+Grotesk:wght@600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="page">
    <header class="topbar">
      <div class="logo">Career Board</div>
      <div class="session">
        <a class="ghost hover:bg-slate-800/60 hover:-translate-y-0.5 hover:shadow-lg transition duration-150" href="/">トップへ戻る</a>
      </div>
    </header>

    <main>
      <section class="hero">
        <div>
          <p class="eyebrow">ログイン</p>
          <h1>アカウントにサインイン</h1>
          <p class="lede">メールアドレスとパスワードでログインします。完了後はダッシュボードへ遷移します。</p>
          <p class="muted">管理者: <code>admin@example.com</code> / `.env` の <code>ADMIN_PASSWORD</code></p>
        </div>
        <div class="card auth-card hover:-translate-y-1 hover:shadow-2xl transition duration-200">
          <form id="loginForm" class="form">
            <label>Email <input name="email" type="email" required placeholder="you@example.com" /></label>
            <label>Password <input name="password" type="password" required /></label>
            <button type="submit">ログイン</button>
            <p class="muted">アカウントをお持ちでない方は <a href="/register">新規登録</a></p>
          </form>
          <div id="authMsg" class="message"></div>
        </div>
      </section>
    </main>
  </div>

  <script type="module" src="/login.js"></script>
</body>
</html>
